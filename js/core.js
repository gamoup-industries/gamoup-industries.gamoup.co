class Game {
    /**
     * @param {object} config
     * @param {number} [config.width=640]
     * @param {number} [config.height=480]
     * @param {HTMLCanvasElement} [config]
     */
    constructor(config) {
        
    }
}